{{ form_start(form, { 'attr': {'style': 'color: red'} }) }}


    <button formnovalidate class="px-4 py-2 text-sm font-medium text-white bg-green-600 border border-transparent rounded-md hover:bg-green-700">
        {{ button_label|default('Save') }}
    </button>
{{ form_end(form) }}
 